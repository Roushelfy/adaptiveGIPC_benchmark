#ifndef __FILE_IO_BMP_IO_LINUX_H__
#define __FILE_IO_BMP_IO_LINUX_H__

#include <stdio.h>
// Remove Windows.h dependency - not needed for Linux
#include "../lib/MY_MATH.h"

// Note: For Linux, we'll use a simpler BMP reading approach
// or rely on external libraries like libpng/libjpeg
// The Windows-specific HBITMAP code is removed

// For now, we'll provide stub functions or alternative implementations
// Users can integrate with stb_image or other cross-platform libraries if needed

#endif
